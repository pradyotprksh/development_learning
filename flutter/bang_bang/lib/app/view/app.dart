import 'package:flutter/material.dart';

/// An App widget which will be the first widget in the widget tree, which will
/// have the MaterialApp and all the other details required at the root.
///
/// Will be used to initialised all the required repositories and helper
/// classes.
///
/// Themes, routes and all will also be added here.
class App extends StatelessWidget {
  /// Constructor for App widget.
  ///
  /// [key] : Key identifier of the widget
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
