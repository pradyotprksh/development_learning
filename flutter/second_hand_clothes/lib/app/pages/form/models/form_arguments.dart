/// A form argument class which will hold the arguments related to form
/// page.
class FormArguments {
  /// [formId] = id of the form to be used
  const FormArguments(
    this.formId,
  );

  final String formId;
}
