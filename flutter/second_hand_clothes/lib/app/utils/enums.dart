enum UtilsAuthenticationStatus {
  unknown,
  authenticated,
  unauthenticated,
}

enum EmailAddressValidationError {
  empty,
  invalid,
  noAccount,
}

enum PasswordValidationError {
  empty,
  tooWeak,
}

