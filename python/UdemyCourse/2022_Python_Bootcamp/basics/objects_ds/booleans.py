from src import log_debug


def booleans_basics():
    """
    Let's check on booleans
    :return:
    """

    # Conveys true or false statements

    log_debug(True)
    log_debug(False)

    log_debug(type(True))

    log_debug(1 > 4)
    log_debug(4 > 1)
    log_debug(4 == 1)
