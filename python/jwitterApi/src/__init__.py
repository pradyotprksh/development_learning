"""
Src module

A module for Jwitter which contains all the logic and implementation insides it.

Only the ones which are required outside are imported here rest can be accessed directly.
"""
from .user.user import User
